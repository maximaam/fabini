{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>{{ page.title(app.request.locale) }}</h1>
                <p>{{ page.description(app.request.locale) }}</p>
            </div>
        </div>
    </div>
{% endblock %}